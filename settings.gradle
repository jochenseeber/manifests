projects = [
    "byte-buddy-annotations",
    "gradle-api-annotations",
    "gradle-bintray-config",
    "gradle-docker-config",
    "gradle-git-config",
    "gradle-github-config",
    "gradle-license-config",
    "gradle-plugin-publish-config",
    "gradle-project-config",
    "gradle-test-kit",
    "gradle-workspace-plugin",
    "gradle-wsimport-demo",
    "gradle-wsimport-plugin",
    "guice-annotations",
    "guice-squeezer",
    "jdk-annotations",
    "junit-annotations",
    "mockito-annotations",
]

projects.each { String project ->
    if(new File(project).exists()) {
	include(project)
    }
}


